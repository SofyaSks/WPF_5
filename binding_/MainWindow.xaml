<Window x:Class="binding_.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:binding_"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" FontSize="48">
    <!--<StackPanel>
        <Slider x:Name="slider"/>
        <TextBox Text="{Binding ElementName=slider, 
            Path=Value, StringFormat=F2}"/>
        <TextBlock Text="{Binding ElementName=slider, 
            Path=Value, StringFormat=F2}"/>
    </StackPanel>-->

    <!--<Window.DataContext>
        <local:Student FirstName="ivan" AverageGrade="4.5"/>
    </Window.DataContext>

    <StackPanel>
        <TextBlock> Имя студента</TextBlock>
        <TextBox Text="{Binding Path=FirstName}"/>

        <TextBlock> Средний балл</TextBlock>
        <TextBox Text="{Binding AverageGrade}"/>
    </StackPanel>-->

    <StackPanel>
        
        <TextBox Text="{Binding Background, RelativeSource={RelativeSource Self}}"/>
        <TextBox/>
    </StackPanel>
</Window>
